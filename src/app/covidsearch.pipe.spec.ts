import { CovidsearchPipe } from './covidsearch.pipe';

describe('CovidsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CovidsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
